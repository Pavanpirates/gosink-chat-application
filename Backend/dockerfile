# Use a stable Java LTS runtime
FROM openjdk:17-jdk-slim

# Set working directory
WORKDIR /app

# Copy Maven wrapper and project files
COPY . .

# Build the Spring Boot JAR
RUN ./mvnw clean package -DskipTests

# Expose the app port
EXPOSE 8080

# Run the JAR
CMD ["java", "-jar", "target/*.jar"]
